import { BotResponse } from '../domain/types';

const sections: string[] = [
  [
    '👤 *Пользователи:*',
    '• Список пользователей — `user:list:read` (по умолчанию: нет).',
    '• ФИО пользователя — по умолчанию: о себе/о другом.',
    '• Изменить ФИО — `user:fullName:write` (по умолчанию: себе).',
    '• Курсы/оценки/тесты пользователя — `user:data:read` (по умолчанию: о себе).',
    '• Роли пользователя — `user:roles:read` (по умолчанию: нет).',
    '• Изменить роли — `user:roles:write` (по умолчанию: нет).',
    '• Статус блокировки — `user:block:read` (по умолчанию: нет).',
    '• Блокировать/разблокировать — `user:block:write` (по умолчанию: нет).'
  ].join('\n'),
  [
    '📚 *Курсы:*',
    '• Список курсов — доступно всем.',
    '• Инфо курса — доступно всем.',
    '• Изменить инфо — `course:info:write` (по умолчанию: своя).',
    '• Список тестов курса — `course:testList` (по умолчанию: своя/записан).',
    '• Активность теста — `course:test:read` (по умолчанию: своя/записан).',
    '• Активировать тест — `course:test:write` (по умолчанию: своя).',
    '• Добавить тест — `course:test:add` (по умолчанию: своя).',
    '• Удалить тест — `course:test:del` (по умолчанию: своя).',
    '• Список студентов — `course:userList` (по умолчанию: своя).',
    '• Записать пользователя — `course:user:add` (по умолчанию: себя).',
    '• Отчислить пользователя — `course:user:del` (по умолчанию: себя).',
    '• Создать курс — `course:add` (по умолчанию: нет).',
    '• Удалить курс — `course:del` (по умолчанию: своя).'
  ].join('\n'),
  [
    '❓ *Вопросы:*',
    '• Список вопросов — `quest:list:read` (по умолчанию: свои).',
    '• Инфо вопроса — `quest:read` (по умолчанию: свои/есть попытка).',
    '• Изменить вопрос — `quest:update` (по умолчанию: свои).',
    '• Создать вопрос — `quest:create` (по умолчанию: нет).',
    '• Удалить вопрос — `quest:del` (по умолчанию: свой).'
  ].join('\n'),
  [
    '🧪 *Тесты:*',
    '• Удалить вопрос из теста — `test:quest:del` (по умолчанию: преподаватель курса).',
    '• Добавить вопрос в тест — `test:quest:add` (по умолчанию: преподаватель и автор вопроса).',
    '• Изменить порядок вопросов — `test:quest:update` (по умолчанию: преподаватель курса).',
    '• Список прошедших тест — `test:answer:read` (по умолчанию: преподаватель курса).',
    '• Оценки пользователей — `test:answer:read` (по умолчанию: преподаватель курса/свои).',
    '• Ответы пользователей — `test:answer:read` (по умолчанию: преподаватель курса/свои).'
  ].join('\n'),
  [
    '📝 *Попытки:*',
    '• Создать попытку — разрешено отвечающему (без разрешения).',
    '• Изменить ответ — разрешено отвечающему (без разрешения).',
    '• Завершить попытку — разрешено отвечающему (без разрешения).',
    '• Посмотреть попытку — `test:answer:read` (по умолчанию: преподаватель курса/свои).'
  ].join('\n'),
  [
    '✅ *Ответы:*',
    '• Создать ответ — системно (без разрешения).',
    '• Посмотреть — `answer:read` (по умолчанию: преподаватель курса/свои).',
    '• Изменить — `answer:update` (по умолчанию: отвечающий).',
    '• Удалить — `answer:del` (по умолчанию: отвечающий).'
  ].join('\n')
];

export const permissionsMessage = (): BotResponse => ({
  messages: sections.map((text) => ({
    text,
    options: { parse_mode: 'Markdown', disable_web_page_preview: true }
  }))
});
